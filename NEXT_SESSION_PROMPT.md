Continuing the MoE AI Center of Excellence case study prototype (Sign Language Academic Video Generator). Your memory should auto-load full project context — check it first (project name: `project-moi-case-study`). Quick status as of 2026-08-18:

- **Step 1 (ZHO indexing + coverage report): done.** See `data/zho/coverage_report.md`.
- **Step 2 (LLM benchmark): model roster finalized in a parallel session** (qwen3:latest, qwen3.5-9b:q4, Falcon-H1-7B-Instruct, jais-adaptive-q4:7b — see memory for full reasoning, including a genuinely strong root-caused finding on a Jais-2/llama.cpp Metal backend bug, and a supplementary TII "Alyah" Emirati-dialect benchmark eval that may have finished running by the time you read this — check `alyah_checkpoint.jsonl` / `alyah_eval_full.log` in the prior session's scratchpad if picking that up, don't restart it from scratch).
- **⚠️ BLOCKER — needs the user, not more agent work:** the actual required grounding-faithfulness benchmark (cosine/ROUGE/Jaccard/BLEU, brief §4) has NOT run and cannot run until the user supplies the real Grade 6 Science Ch.3 source text. The Alyah eval above is supplementary "Model Selection" narrative material, not a substitute. **Ask the user for the Ch.3 text before anything else in step 2.**
- **MoE HR answered 3 open questions by email (2026-08-18)** — captured in memory: slides can be English or Arabic (candidate's choice); prototype video can be an animated avatar OR a realistic AI-generated/deepfake person, candidate's choice, framed explicitly around "whichever is financially better in the long run" (reinforces the brief's existing deterministic-clip-assembly decision — cite this email on the architecture slide); Arabic Sign Language does not need to be specifically Emirati — standard/official ArSL is fine, ZHO remains the right primary source but gaps can be supplemented from broader ArSL resources if needed.
- **Avatar side-experiment (not a required deliverable, explored but not concluded)** — read `data/zho/spike_mediapipe/AVATAR_HANDOFF.md` before touching this again if asked. Current safe fallback is a simplified procedural cartoon renderer, no headwear/costume detail.

**Do not restart or duplicate work already done** — read the coverage report, memory, and the avatar handoff doc before making any changes in those areas.

**Next step per the brief's build order: Section 3, backend pipeline** (FastAPI: `source_ingest`, `understand`, `structure`, `generate`, `validate`, `assemble`), once step 2's real benchmark numbers are confirmed in — check with the user which they want to start on.
